# DS-WEB

Aula-10:
- include: acrescenta os códigos (parte) de um arquivo já pronto, sendo necessário colocar a tag e o arquivo específico que deseja incluir. Obs: Apostila "é o meio mais funcional para agrupar códigos.";
-A expressão também pode ser utilizada juntamente com o if, exemplo, if maior que 18 Include (arquivo);
-Deve-se sempre usar o <?p   ...    ?>;
-Include_once(): verifica se o arquivo já foi inserido no código para que não ocorra repetição. Quando não encontra o arquivo ele avisa;
- require_once(): ao não encontrar o arquivo mencionado ele encerra a programação e a execusão da página.
-->
"# DS-WEB"