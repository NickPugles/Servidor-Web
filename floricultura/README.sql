# DS-WEB

- Banco de Dados (Comandos):

- Create database floricultura;

- Create table usuario(
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100),
    email VARCHAR(100),
    senha INT
);

- Create table produtos(
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100),
    descricao VARCHAR(100),
    preco INT,
    quantidade INT
);

-ISERT INTO usuario(nome, email, senha)
values('Nicolli' 'nicolli@nicolli.com'123);
-->
"# DS-WEB" 
